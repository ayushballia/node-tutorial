// CommonJS, every file is module (by default)
// Modules - incapsulated Code (only share minimum)

const names = require("./4-name");
const sayHI = require("./5-utils");
const data = require("./6-alternative-flavor");
require("./7-mind-granade");

// sayHI("susan");
// sayHI(names.john);
// sayHI(names.peter);
